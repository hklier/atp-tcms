# Apply this file manually after replacing every placeholder. Never commit the real file.
apiVersion: v1
kind: Secret
metadata:
  name: kiwi-credentials
  namespace: atp-tcms
type: Opaque
stringData:
  mariadb-root-password: REPLACE_WITH_A_LONG_RANDOM_VALUE
  mariadb-password: REPLACE_WITH_A_LONG_RANDOM_VALUE
  django-superuser-username: admin
  django-superuser-email: REPLACE_WITH_ADMIN_EMAIL
  django-superuser-password: REPLACE_WITH_A_LONG_RANDOM_VALUE
  kiwi-domain: REPLACE_WITH_TCMS_FQDN_OR_LOADBALANCER_IP

