apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb-data
  namespace: atp-tcms
  labels:
    app.kubernetes.io/part-of: atp-tcms
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

